import { StyleSheet } from "react-native";

const styles = StyleSheet.create ({
    container: {
        padding: 5,
        flexDirection: 'row',
        width: 'fit-content',
        marginHorizontal: 10,
    },
    img: {
        height: 180,
        width: 130,
        borderRadius: 10,
    },
});

export default styles;