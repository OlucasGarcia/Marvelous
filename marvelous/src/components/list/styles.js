import { StyleSheet } from "react-native";

const styles = StyleSheet.create ({
    container: {
        borderWidth: 10,
        borderColor: 'blue',
        flexWrap: 'wrap',
        flexDirection: 'row',
    }
});

export default styles;